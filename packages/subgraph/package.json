{
  "name": "@spirith/subgraph",
  "version": "0.0.0",
  "private": true,
  "description": "Spirith subgraph: ENSv2 registrar and registry plus SpirithVault events on Sepolia, deployed to Subgraph Studio.",
  "scripts": {
    "prepare": "node scripts/prepare.mjs",
    "codegen": "pnpm prepare && graph codegen",
    "build": "pnpm codegen && graph build",
    "check-types": "pnpm build",
    "deploy:studio": "pnpm build && graph deploy spirith-sepolia --deploy-key $GRAPH_DEPLOY_KEY --version-label $(node scripts/version-label.mjs)"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "catalog:",
    "@graphprotocol/graph-ts": "catalog:"
  }
}
